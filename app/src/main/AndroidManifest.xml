<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mi.primer.myrecetario">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/iconopan"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconopan"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myrecetario"
        tools:targetApi="31">

        <!-- Meta-datos para Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Definición de actividades -->
        <activity android:name=".CreaTuTarea" android:exported="false" />
        <activity android:name=".ListaDeTareas" android:exported="false" />
        <activity android:name=".Provicional" android:exported="false" />
        <activity android:name=".CreationRecetaOriginal" android:exported="false" />
        <activity android:name=".CreationRecetaRollo" android:exported="false" />
        <activity android:name=".CreationRecetaGalletas" android:exported="false" />
        <activity android:name=".CreationRecetaPasteles" android:exported="false" />

        <activity android:name=".CreationRecetaBatidos" android:exported="false" />
        <activity android:name=".CreationReceta" android:exported="false" />

        <activity android:name=".WelcomeSession" android:exported="false" />
        <activity android:name=".CreationUser" android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
