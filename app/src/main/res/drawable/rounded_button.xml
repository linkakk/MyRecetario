<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo blanco -->
    <solid android:color="@color/buttons" />

    <!-- Bordes redondeados -->
    <corners android:radius="16000dp" />

    <!-- Borde alrededor del shape -->
    <stroke
        android:width="2dp"
    android:color="@android:color/black" /> <!-- Color del borde -->
</shape>
